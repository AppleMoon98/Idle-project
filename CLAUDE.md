# Project Specification: 2D Idle RPG (Unity 6)

## 1. Project Overview
- **Genre:** 2D Idle RPG (Medieval Fantasy)
- **Target Platform:** Mobile (Android / iOS)
- **Engine/Lang:** Unity 6 / C#
- **Key Mechanics:** Auto-combat, Monster Spawning, Equipment & Growth, Stage Escalation, Story-driven Rank System, Climax War Battles.

## 2. Core Game Systems

### A. Stage & Combat Mechanics
- **Stage Progression:** Chapter-based structure (e.g., 1-1 ~ 1-40, 2-1 ~ 2-40).
- **Combat Loop:**
  - Monsters spawn off-screen and move toward the Player.
  - Player attacks with equipped weapons.
  - On contact with player -> Player HP decreases.
  - Progression Loop: Auto-Combat -> Gold/Equipment Drop -> Enhance Equipment -> Stat Growth -> Stage Clear -> Unlock Rank/Content.

### B. Rank & Progression System
- Player progresses from "시골 소년" to "병사", "십인 대장", etc.
- Unlocks occur at stage milestones (e.g., Clearing 4-40 -> Rank Upgrade -> Short Dialogue Cutscene -> Unlocks New System).
- System unlocks are gated by **Rank**, NOT Player Level.

### C. War & Soldier Systems
- **Soldier System:** Unlocked at 'Decurion'. Spawns small auxiliary troops that fight alongside the player (does not alter the core idle RPG genre).
- **War System:** Triggered at chapter climaxes (e.g., Stage X-40). Features larger monster/soldier scale and visual effects without changing genre mechanics.

### D. Growth Systems (Decoupled Subsystems)
- Equipment, Skills, Pets, Relics, Collections, Achievements, Enhancements, Promotions.
- Every growth subsystem must be independent and decoupled.

## 3. Architecture & Technical Rules

### A. Programming Guidelines
- **Design Pattern:** Object-Oriented Programming (OOP), SOLID, Composition over Inheritance.
- **Data Architecture:** Data-driven via ScriptableObjects (SO).
- **Communication:** Event-driven architecture (Observer/Action based). Cross-system communication goes through `Core.EventBus` — domain systems must never reference each other directly.
- **Performance:** Avoid `Update()` abuse, strict Object Pooling, Addressables-ready design.
- **Code Style:** XML Documentation comments, zero magic numbers, interface-driven.

### B. Directory Structure
```
Assets/
├── 02. Script/
│   ├── Core/
│   ├── Character/
│   ├── Combat/
│   ├── Stage/
│   ├── War/
│   ├── Soldier/
│   ├── Equipment/
│   ├── Inventory/
│   ├── UI/
│   ├── Save/
│   ├── Offline/
│   ├── Managers/
│   ├── Services/
│   └── Editor/
└── 03. SO/
    ├── Characters/
    ├── Monsters/
    ├── Items/
    ├── Skills/
    ├── Stages/
    └── Configs/
```

### C. Core Foundation (implemented)
`Assets/02. Script/Core/` provides the base every other system builds on:
- `IManager` — lifecycle contract (`Initialize`/`Shutdown`) for stateful managers.
- `IService` — marker interface for stateless functionality providers.
- `ITickable` — `Tick(float deltaTime)` contract; register with `GameTicker` instead of writing a per-object `Update()`.
- `ServiceLocator` — type-keyed instance registry (`Register<T>`/`Get<T>`/`TryGet<T>`/`Unregister<T>`). Replaces scattered static singletons.
- `EventBus` — typed pub/sub (`Subscribe<T>`/`Unsubscribe<T>`/`Publish<T>`) for decoupled cross-system communication.
- `GameTicker` — the one `MonoBehaviour` allowed to own an `Update()`; ticks all registered `ITickable`s, with safe register/unregister during iteration.
- `GameBootstrapper` — scene entry point (composition root). `Awake()` creates `ServiceLocator`/`EventBus`, registers `EventBus`, `GameTicker`, and `PoolManager` into the locator, exposes `static Services`/`static Events` as the single intentional global access point.

Domain systems (Combat, Stage, War, Equipment, ...) depend on `Core`; `Core` never depends on them. Domain systems talk to each other only via `EventBus`, never by direct reference.

### D. Object Pooling (implemented)
`Assets/02. Script/Core/Pooling/` + `Assets/02. Script/Managers/PoolManager.cs`:
- `IPoolable` — optional `OnSpawned()`/`OnDespawned()` hooks for pooled components.
- `PooledInstance` — internal tag component recording a spawned instance's source prefab, so callers can `Release(instance)` without remembering which pool it came from.
- `ObjectPool<T>` — generic `Stack<T>`-based pool; create/get/release/destroy are all caller-supplied callbacks, capacity/max size are constructor params (no magic numbers).
- `PoolManager` (`IManager`, `IService`) — keeps one `ObjectPool<GameObject>` per registered prefab. `RegisterPool(prefab, defaultCapacity, maxSize)`, `Get(prefab, position, rotation)`, `Release(instance)`. Registered into `GameBootstrapper.Services` at startup.
- Any system that repeatedly spawns/destroys GameObjects (monsters, projectiles, damage numbers, soldiers, VFX) must go through `PoolManager` instead of `Instantiate`/`Destroy` directly.

### E. Character Foundation (implemented)
`Assets/02. Script/Character/` — the shared component set Player and Monster are both composed from (no inheritance hierarchy):
- `CharacterStatsSO` — data asset (`MaxHealth`/`AttackPower`/`MoveSpeed`/`AttackInterval`), read-only, never mutated at runtime.
- `RuntimeStats` — plain C# copy of a `CharacterStatsSO`'s values with settable properties, so buffs/equipment can modify it later without touching the source asset.
- `CharacterStatsProvider` — holds the `CharacterStatsSO` reference and lazily builds/exposes `RuntimeStats` via `Stats` (lazy so it doesn't depend on component `Awake()` order).
- `Health` (`IPoolable`) — `TakeDamage`/`Heal`, clamps to `[0, MaxHealth]`, publishes `CharacterHealthChangedEvent` on change and `CharacterDiedEvent` on death via `GameBootstrapper.Events`. `OnSpawned()` resets current health and `IsDead` for pooled reuse.
- `CharacterMover` (`ITickable`) — moves toward a `Target` transform at `Stats.MoveSpeed`; registers with `GameTicker` in `OnEnable`. Doesn't know about "Player" — the target is assigned by whichever system spawns it (Stage/Monster spawner, later).
- `Character/Events/CharacterHealthChangedEvent.cs`, `CharacterDiedEvent.cs` — `readonly struct` events other systems (Stage, Combat, UI) subscribe to; `Health` never references those systems directly.
- `Health.Revive()` — public API that resets `IsDead`/heals to max (same logic as `OnSpawned()`, extracted so non-pooled characters can use it too). `OnSpawned()` now just calls `Revive()`.
- `PlayerReviveOnStageChanged` (MonoBehaviour, Player only) — subscribes `Stage.Events.StageChangedEvent`, calls `Revive()` on every stage change (advance, repeat, or death-fallback). Character doesn't know Stage exists as a system; it just reacts to Stage's public event, same direction as `StatEnhancementReceiver` reacting to `Enhancement`.
- `HealthBarUI` (MonoBehaviour) — world-space health bar; hidden until first damage (`Current < Max`), shown/updated via `CharacterHealthChangedEvent`, `fillAmount` tweens smoothly (`ITickable`, `GameTicker`) instead of snapping so fast deaths are still visible. Nested as a child prefab (`Assets/04. Prefab/HealthBar.prefab`) inside `Monster.prefab`/`Soldier.prefab`/`Soldier_Ranged.prefab`. **Gotcha:** the fill `Image`'s `Source Image` must be non-null and `Type` must be `Filled` (enum value `3`, not `1` — `Simple=0, Sliced=1, Tiled=2, Filled=3`) or `fillAmount` is silently ignored and the bar always renders full.

Note: `CharacterStatsSO`/`RuntimeStats` also carry `AttackRange` (added for Combat).

### F. Combat (implemented)
`Assets/02. Script/Combat/` — 2D physics-based auto-combat, built only on `Health`/`RuntimeStats`/`GameTicker`; never references Stage/Rank/Loot:
- `Attacker` (`ITickable`) — range-scan attacker. Accumulates `deltaTime`; every `Stats.AttackInterval`, runs `Physics2D.OverlapCircleAll(position, Stats.AttackRange, targetLayerMask)`, picks the nearest live `Health`, calls `TakeDamage(Stats.AttackPower)`. Used by Player to auto-attack Monsters.
- `ContactAttacker` (`ITickable`) — trigger-based attacker. Tracks the `Health` currently overlapping via `OnTriggerEnter2D`/`OnTriggerExit2D` (2D triggers — this is a 2D mobile game, no 3D physics), attacks it every `Stats.AttackInterval` while in contact. Used by Monsters to damage the Player on contact.
- Neither component publishes its own events or knows about factions by name — target filtering is purely via `LayerMask` set per-prefab in the Inspector (Player prefab targets the Monster layer, Monster prefab targets the Player layer), so adding Soldier/War later needs no Combat code changes.
- Damage application and death/health events remain owned by `Character.Health` — Combat only decides *who* and *when* to hit.
- `EnemyTracker` (`ITickable`, requires `CharacterStatsProvider`+`CharacterMover`) — periodically (`retargetInterval`) scans `detectionRange` for the nearest live, on-screen `Health` and sets it as the `CharacterMover.Target` (with `StoppingDistance = AttackRange`), so the character walks up and `Attacker` takes over once in range. Player and Soldier both use this instead of a fixed target — nobody stands still except Monsters walking straight at whatever `MonsterTargetSelector` picks (below).
- `ITargetFilter` — optional hook a component can implement (`GetComponent<ITargetFilter>()` in `EnemyTracker.Awake`) to bias which candidate is preferred without changing `EnemyTracker` itself. `Soldier.SoldierEnemyFilter` implements it so soldiers spread out across different monsters instead of piling onto the same one (see Soldier section). If no candidate is preferred, `EnemyTracker` falls back to plain nearest.
- `PlayerTargetTracker` (`IManager`/`IService`) — single-slot record of whichever `Health` the Player's `EnemyTracker` currently has targeted.
- `PlayerTargetMarker` (`ITargetFilter`, Player only) — always returns `true` for `IsPreferred` (doesn't change Player's own targeting), just mirrors `EnemyTracker`'s final pick into `PlayerTargetTracker` via `OnTargetAcquired`.
- `MonsterTargetSelector` (`ITickable`, Monster only) — replaces the old "always walk at the Player" behavior. Every `retargetInterval`, re-evaluates `CharacterMover.Target` by priority: (1) if `PlayerTargetTracker.CurrentTarget == 나` → Player; (2) else if `SoldierTargetRegistry.TryGetClaimant(나)` finds a Soldier currently targeting this monster → that Soldier; (3) else → Player (fallback). `MonsterSpawner` injects the Player `Transform` via `Initialize()` right after spawn instead of setting `CharacterMover.Target` directly.

### G. Stage (implemented)
`Assets/02. Script/Stage/` — runs a single stage (e.g. "1-1") and detects clear; knows nothing about Rank/UI, and only touches `Equipment` for the drop-table data type (see Loot section):
- `StageSO` — data asset: `Chapter`, `StageNumber`, `SpawnEntries` (`MonsterSpawnEntry[]`), `EquipmentDrops` (`Equipment.EquipmentDropEntry[]` — the stage's equipment drop table, see Loot section; empty by default, e.g. chapters 1–5 have none by design, drops start at 6-1).
- `MonsterSpawnEntry` (`[Serializable]`, not a SO) — one spawn wave: `MonsterPrefab`, `Count`, `SpawnInterval`.
- `MonsterSpawner` (`ITickable`) — walks `SpawnEntries` in order, spawns via `PoolManager.Get`, and calls `StageProgressTracker.RegisterSpawned` for each instance. Monsters always spawn off-screen opposite the player: it picks a top or bottom spawn point (`topSpawnPoints`/`bottomSpawnPoints`, both Inspector-assigned `Transform[]`) based on whether the player is currently near the top of the viewport (`playerNearTopViewportThreshold`). Instead of setting `CharacterMover.Target` directly, it calls `MonsterTargetSelector.Initialize(playerTransform)` on the spawned instance (see Combat section) so the monster's real target gets decided by the priority system.
- `StageProgressTracker` — tracks exactly the monsters it was told about via `RegisterSpawned` (a `HashSet<GameObject>`, not tag/string based) against `CharacterDiedEvent`; when the registered count all die, publishes `StageClearedEvent` and unsubscribes itself (`Dispose`). `ReleaseRemaining(PoolManager)` force-releases any still-alive monsters without loot/events — used when a stage is abandoned mid-fight (death fallback) rather than cleared.
- `Stage/Events/StageClearedEvent.cs` — `readonly struct` carrying the cleared `StageSO`; consumed by `StageProgression` (see Stage Progression section).
- `StageCatalogSO` — ordered `StageSO[]` array defining stage sequence; `Find(chapter, stageNumber)`, `GetNext(current)`, plus index-based `IndexOf(stage)`/`GetAt(index)` used by `StageProgression`'s breakthrough/repeat math.
- `StageController` (MonoBehaviour, composition root for one stage) — loads `SaveData` once (`LoadSave()`), resolves the initial stage from it (`ResolveInitialStage`), and constructs `StageProgression` with the saved current + highest-cleared stages and a `[SerializeField] maxRegressionDistance` (default 20). `LoadStage(stageSO)` calls `PoolManager.EnsurePool` per monster prefab (idempotent — see below), builds `StageProgressTracker` + `MonsterSpawner`, registers the spawner with `GameTicker`, publishes `StageChangedEvent`. `EndCurrentStage()` now also calls `StageProgressTracker.ReleaseRemaining` before disposing, so switching stages (including a death-triggered fallback) never leaves stray monsters from the abandoned attempt.
- **`PoolManager.EnsurePool(prefab, defaultCapacity, maxSize)`** (added to `Managers/PoolManager.cs`) — registers a pool only if one doesn't already exist for that prefab; needed because multiple stages reuse the same monster prefabs and `RegisterPool` throws on duplicate registration.

See "Stage Progression: Breakthrough & Repeat" section for how `StageController.LoadStage` gets called across stage clears/deaths.

### H. Attack Behaviors: Melee / Ranged (implemented)
`Assets/02. Script/Combat/` — pluggable "what happens on hit" strategies for `Attacker`, swapped by composition (attach the desired behavior component, no subclassing):
- `IAttackBehavior` — `Execute(origin, target, attackPower)`. `Attacker.Awake()` caches `GetComponent<IAttackBehavior>()` and calls it instead of applying damage directly; if none is attached, the attack silently no-ops.
- `WeaponSwing` (`ITickable`) — attached to a "weapon socket" child transform (e.g. `Player/WeaponAnchor`). `Play()` rotates the socket 0→`swingAngle`→0 over `swingDuration` via a sine curve, ticking only while actively swinging. This anchor is where the future Equipment system will parent the equipped weapon's visual — swinging the socket swings whatever is attached to it, without Combat needing to know about Equipment.
- `MeleeAttackBehavior` — applies damage immediately (hit detection stays in `Attacker`'s range scan) and calls `WeaponSwing.Play()` for the visual. The swing is purely cosmetic and does not gate the hit.
- `Projectile` (`ITickable`) — spawned by `RangedAttackBehavior`, homes toward a captured `Health` target each tick, applies damage and self-releases to the pool on arrival (`hitDistance`) or if the target dies/vanishes first (fake-null-safe).
- `RangedAttackBehavior` — spawns/launches a `Projectile` from `PoolManager` instead of dealing damage directly; damage lands only when the projectile arrives.
- Both `Character/Monster` test prefabs demonstrate this: Player currently has `MeleeAttackBehavior` wired to `WeaponAnchor`'s `WeaponSwing`; `RangedAttackBehavior` + `Assets/04. Prefab/Projectile.prefab` were verified working the same way (swap the attached behavior component to switch).

**Known gotcha — Script Execution Order:** Unity processes `Awake`→`OnEnable` per-GameObject in scene order, not as two separate global passes. Any component that reads `GameBootstrapper.Services`/`Events` in `OnEnable` (a one-shot check) can silently and permanently fail to acquire it if that GameObject initializes before `GameBootstrapper`'s GameObject does (this actually happened with `RangedAttackBehavior` failing to get `PoolManager`). Fix applied: `GameBootstrapper`'s script execution order is set to `-1000` (stored in `Core/GameBootstrapper.cs.meta`, committed) so its `Awake()` always runs first. Keep this in mind if new components read `GameBootstrapper.Services`/`Events` in `OnEnable`/`Awake`.

### I. Loot & Currency (implemented)
`Assets/02. Script/Loot/` — turns monster death into gold/equipment drops; knows Character (via `CharacterDiedEvent`), `Stage` (via `StageChangedEvent` + `StageCatalogSO`, for the current equipment drop table), and the `Equipment` data type:
- `MonsterLootSO` — data asset, **gold only**: `MinGold`/`MaxGold`/`DropChance`. Equipment is no longer per-monster — it moved to `StageSO.EquipmentDrops` (see Stage section) because drop tables are designed per stage (e.g. nothing drops before 6-1), not per monster species.
- `Equipment.EquipmentDropEntry` (`[Serializable]`, not a SO, lives in the `Equipment` namespace/folder even though `Loot` also uses it) — one drop table row: `EquipmentSO Equipment`, `DropChance`.
- `MonsterLootProvider` (MonoBehaviour) — attached to monster prefabs, holds the `MonsterLootSO` reference so `LootDropper` can identify "this dead GameObject was a monster with loot" without any tag/name check.
- `LootDropper` (plain C# class, constructed once in `GameBootstrapper` with a `StageCatalogSO`, not per-stage) — subscribes `CharacterDiedEvent` for drops and `StageChangedEvent` to track the current `StageSO` (resolved via `catalog.Find(chapter, stageNumber)`). On a monster's death: gold rolls against that monster's own `MonsterLootSO`, equipment rolls against the *current stage's* `EquipmentDrops` — both independently, both via `ItemDroppedEvent`/`GoldEarnedEvent`.
- `LootRoller` (static) — pure probability-rolling logic shared by `LootDropper` (real-time) and `OfflineProgressService` (simulated), so both roll with identical odds: `RollGold(MonsterLootSO)`, `RollEquipment(EquipmentDropEntry[])`.
- `CurrencyService` (`IManager`, `IService`) — subscribes `GoldEarnedEvent`, accumulates `CurrentGold`, publishes `GoldChangedEvent` on any change. Also exposes `TrySpendGold(amount)` (used by `Enhancement`) which fails without side effects if the balance is insufficient.
- `Loot/Events/GoldEarnedEvent.cs`, `GoldChangedEvent.cs`, `ItemDroppedEvent.cs` — the three event types other systems subscribe to.

### J. Equipment (implemented)
`Assets/02. Script/Equipment/` — item data model plus the fusion/enhancement growth loop. Equip *state* (which owned line is worn) lives in `Inventory` instead (see Inventory section) since it's about ownership, not item definitions.

**Data model**
- `EquipmentType` — `enum { Weapon, Gloves, Armor, Helmet, Shoes }` (5 gear slots).
- `EquipmentSO` — `ItemName`, `EquipmentType` (slot), `Grade` (`EquipmentGradeSO`, which rung of the grade ladder this specific slot+grade item is), `Icon` (added alongside section AI's Content Editor tool — not yet drawn by any existing UI, just the field + a place to fill it in for now).
- `EquipmentGradeSO` — one rung of the grade ladder (e.g. "커먼1"): `DisplayName`, `TintColor`. Deliberately has no separate "main grade" (Common/Rare/...) + "sub-tier" fields — the grade's position is purely its index in `EquipmentGradeCatalogSO`, so grades are entirely content-driven (add/reorder SO assets, no code/enum changes needed to extend the ladder).
- `EquipmentGradeCatalogSO` — ordered `EquipmentGradeSO[]` (currently 30: 커먼1–5, 언커먼1–5, 레어1–5, 슈퍼레어1–5, 에픽1–5, 레전더리1–5). Same shape as `StageCatalogSO`: `IndexOf`/`GetAt`/`GetNext`.
- `EquipmentCatalogSO` — flat `EquipmentSO[]` of every item definition (currently 150 = 5 slots × 30 grades); `FindBySlotAndGrade(slot, grade)` is how fusion finds "the next tier of this same slot." `IndexOf(item)`/`GetAt(index)` (same shape as the other catalogs) let `SaveService` identify an item by a plain int instead of an asset reference for PlayerPrefs storage (see Save section).

**Growth loop**
- `EquipmentFusionService` (`IManager`/`IService`) — `TryFuse(definition)` consumes 5 copies of the same line (via `InventoryService.TryConsume`) and grants 1 copy of `equipmentCatalog.FindBySlotAndGrade(definition.EquipmentType, gradeCatalog.GetNext(definition.Grade))` by re-publishing `ItemDroppedEvent` (reuses the normal acquisition pipeline — fusion output isn't special-cased in `InventoryService`). Fails harmlessly if already max grade or the next-tier item doesn't exist in the catalog yet (content gap).
- `EnhancementStoneService` (`IManager`/`IService`) — a second currency, same shape as `CurrencyService`/Gold (`AddStones`/`TrySpendStones`, publishes `EnhancementStoneChangedEvent`). Nothing grants stones yet — reserved for a future "강화석 던전" (enhancement stone dungeon); the spend side is built now so that system just needs to call `AddStones` later, no rework.
- `EquipmentEnhancementConfigSO` — one shared data asset (not per-grade/per-slot): `DuplicatesRequiredPerLevel`, `StoneCostBase`, `StoneCostIncreasePerLevel`, `StatBonusPerLevel`, `MaxLevel`.
- `EquipmentEnhancementService` (`IManager`/`IService`) — `TryEnhance(definition)` consumes `DuplicatesRequiredPerLevel` copies (keeping at least 1 — the one being enhanced isn't itself consumed) plus a scaling stone cost, then calls `InventoryService.AddEnhancementLevel`. `StatBonusPerLevel` is stored but not yet *applied* to the player — that lands whenever the equip system starts granting stats on wear.

### K. Inventory (implemented)
`Assets/02. Script/Inventory/` — holds owned equipment by line (not by individual copy) and tracks what's equipped; knows nothing about `Loot` except the event types it subscribes to:
- `OwnedEquipment` (plain class, not a SO — runtime owned state) — `Definition` (`EquipmentSO`), `Count` (stack size, used as fusion/enhancement material and for UI "×N"), `EnhancementLevel`. One `OwnedEquipment` per distinct `EquipmentSO`; duplicates increment `Count` rather than creating separate entries, and `EnhancementLevel` is shared by the whole stack (there's no per-copy enhancement — enhancing "spends" other copies of the same line as material to level up the one line).
- `InventoryService` (`IManager`, `IService`) — internally a `Dictionary<EquipmentSO, OwnedEquipment>`, exposed read-only as `Items`. Subscribes `ItemDroppedEvent` (increments existing line's `Count` or creates a new line). `TryGet(definition, out owned)`, `TryConsume(definition, amount)` (fails without side effects if short; removes the line entirely if it hits 0), `AddEnhancementLevel(definition, levels)` — all publish `InventoryChangedEvent(changed, totalDistinctItems)` on success so UI can refresh. `ExportSnapshot(catalog)`/`RestoreSnapshot(snapshot, catalog)` (de)serialize the whole dictionary to/from `OwnedEquipmentSnapshot[]` (catalog index + count + enhancement level) for `SaveService`; restoring does not publish `InventoryChangedEvent` (it's seeding, not a gameplay change).
- `EquippedGearService` (`IManager`, `IService`) — `Dictionary<EquipmentType, OwnedEquipment>` of what's worn per slot. `Equip(owned)` just records the reference and publishes `EquipmentEquippedEvent(slot)` — it does **not** consume `Count`; equipping doesn't remove the item from the stack, it just points at one of the copies as "currently worn." `ExportSnapshot(catalog)`/`RestoreSnapshot(snapshot, catalog, inventory)` mirror `InventoryService`'s for `SaveService`, keyed by `EquipmentType` + catalog index; restore looks the definition up in the (already-restored) `InventoryService` rather than creating its own `OwnedEquipment`.
- `Inventory/Events/InventoryChangedEvent.cs` (`OwnedEquipment Changed`, `int TotalDistinctItems`), `EquipmentEquippedEvent.cs` (`EquipmentType Slot`, no payload — subscribers re-query `EquippedGearService` for details).

### L. Enhancement (implemented)
`Assets/02. Script/Enhancement/` — spends gold to raise the player's base stats; deliberately does not know "Player" exists:
- `EnhancementStatType` — `enum { AttackPower, MaxHealth }`.
- `EnhancementConfigSO` — per-stat data asset: `StatType`, `BaseCost`, `CostIncreasePerLevel`, `ValuePerLevel`, `MaxLevel`. Cost for the next level is `BaseCost + CostIncreasePerLevel * currentLevel` — fully data-driven, no magic numbers in code.
- `EnhancementService` (`IManager`, `IService`) — constructed with `EventBus`, `CurrencyService`, and the full `EnhancementConfigSO[]` (assigned on `GameBootstrapper` in the Inspector). `TryEnhance(statType)` computes the next cost, calls `CurrencyService.TrySpendGold`, and on success increments the internal level and publishes `StatEnhancedEvent` (stat type + `ValuePerLevel` to apply + new level). Never touches any `RuntimeStats` itself.
- `Enhancement/Events/StatEnhancedEvent.cs` — the event `Character.StatEnhancementReceiver` subscribes to.
- `Character/StatEnhancementReceiver.cs` (MonoBehaviour, attached to the Player) — subscribes `StatEnhancedEvent`, adds `ValuePerLevel` to its own `CharacterStatsProvider.Stats.AttackPower`/`MaxHealth` depending on `StatType`. This is how "Enhancement doesn't know Player" and "Player's stats still get buffed" coexist — the receiver lives on the Character/Player side, not the Enhancement side.

### M. UI Shell (implemented)
`Assets/02. Script/UI/` — mobile-portrait presentation layer; every component here subscribes only to the events of the one domain it displays, never to each other:
- `Canvas`'s `CanvasScaler` is set to `ScaleWithScreenSize`, reference resolution 1080x1920 (portrait).
- `BottomMenuUI` — generic tab-bar controller: an inspector-assigned `(Button, GameObject panel)[]` array. Clicking a tab's button opens its panel (closing whichever was open); clicking the already-open tab's button closes it. Has zero knowledge of what any panel contains. The 장비 tab's entry is `EquippedSlotBar`, not a dedicated list panel (see `EquippedSlotBarUI` below) — `EquipmentPanel`/`EquipmentPanelUI` are left in the scene, unwired and inactive, in case a full-list view is wanted again later.
- `GoldDisplayUI` — subscribes `GoldChangedEvent`; reads `CurrencyService.CurrentGold` once on `OnEnable` for the initial value (since the event only fires on change, not on load).
- **Gotcha (legacy `Text` silently renders nothing):** if a `Text`'s `RectTransform` height is shorter than what its `fontSize` needs for one line (`preferredHeight`), `Vertical Overflow = Truncate` drops the *entire* line's vertices — the text is fully populated and the component looks correctly configured, but `vertexCount` is 0 and nothing draws. This hit `GoldText` (box height 60 vs. `fontSize` 72 needing ~80) and looked exactly like "the element went off-screen" from a resolution change; it hadn't — check `preferredHeight` vs. box height (or just set `Vertical Overflow = Overflow`) before suspecting layout/anchors.
- `StageInfoUI` — subscribes `Stage.Events.StageChangedEvent` + `Stage.Events.StageProgressChangedEvent`; shows "스테이지 N-M" + "남은 몬스터: X/Y". `Stage.StageProgressTracker` publishes `StageProgressChangedEvent(remaining, total)` once at construction (remaining = total) and again on every `CharacterDiedEvent` kill it registers, so remaining-count UI doesn't need to know about spawn waves at all.
- `EquipmentRowUI` — one card row shared by every place that lists owned equipment (`EquipmentSlotPopupUI`, `EquipmentPanelUI`): serialized refs to its background `Image`, label `Text`, and three `Button`s (row/fuse/enhance), wired directly to `EquippedGearService.Equip`/`EquipmentFusionService.TryFuse`/`EquipmentEnhancementService.TryEnhance` in `Initialize(owned, isEquipped, backgroundColor, onEquipped)`. `EquipmentRowUI.ComputeGradeBackground(cardBaseColor, grade, blend)` is a shared `static` helper (`Color.Lerp` toward `EquipmentGradeSO.TintColor`) so every list uses the same grade-tint formula.
- `EquipmentPanelUI` — subscribes `InventoryChangedEvent` + `EquipmentEquippedEvent`; instantiates one `EquipmentRowUI` per owned line, sorted by slot then grade (`EquipmentGradeCatalogSO.IndexOf`), across *all* slots (not filtered like the popup).
- `StatPanelUI` — subscribes `StatEnhancedEvent`; shows each stat's current level and next cost via `EnhancementService`, and wires its two buttons to `EnhancementService.TryEnhance`.
- `EquippedSlotBarUI` + `EquipmentSlotPopupUI` — a bar of 5 slot buttons (무기/장갑/갑옷/투구/신발 order), each grade-tinted via a `Frame` `Image` (`EquippedGearService.GetEquipped(slot).Definition.Grade.TintColor`, or a neutral grey when empty). `EquippedSlotBar` is now hidden until the 장비 bottom tab opens it (see `BottomMenuUI` above) rather than always-visible; its `OnEnable` auto-opens the popup for `slots[0].Type` (무기) so the tab is one tap into a browsable list, and its `OnDisable` explicitly calls `EquipmentSlotPopupUI.Close()` since the popup is a Canvas sibling, not a child, and wouldn't otherwise close with the bar. The popup lists that slot's `InventoryService.Items` filtered by slot and sorted by grade, one `EquipmentRowUI` (`EquipmentRow.prefab`) instance per line; tapping a row equips and closes the popup, and its 합성/강화 buttons call fusion/enhancement directly without closing.
- **Gotcha (equip indicator goes stale without `EquipmentEquippedEvent`):** equipping something doesn't change owned counts, so it only publishes `EquipmentEquippedEvent`, never `InventoryChangedEvent`. Any list that shows a "✓ 장착" tag (`EquipmentPanelUI`, `EquipmentSlotPopupUI`) must subscribe to *both* events and refresh on either — subscribing only to `InventoryChangedEvent` (as both did originally) leaves the checkmark on whatever was equipped when the list last rebuilt, until something else happens to trigger a real inventory change.
- `SoldierPanel`/`RelicPanel` — scriptless placeholder panels ("준비 중") since the Relic system doesn't exist yet (Soldier got its real controller in section N; Skill got its own in section AH). Swap in a real controller when Relic is built, following the same subscribe-only-your-domain pattern.
- **Gotcha (new Input System projects):** UI `Button` clicks need an `EventSystem` + an input module in the scene. This project has `activeInputHandler: 1` (new Input System only, old Input Manager disabled), so the module must be `InputSystemUIInputModule`, not the legacy `StandaloneInputModule` — the legacy one silently fails to deliver clicks with the old Input Manager off.
- **Gotcha (teardown order):** any UI `MonoBehaviour` that touches `GameBootstrapper.Events`/`Services` in `OnDisable`/`OnDestroy` must null-conditional it (`GameBootstrapper.Events?.Unsubscribe(...)`). On exiting Play Mode, `GameBootstrapper.OnDestroy` can run and null out `Events` before a UI GameObject's own `OnDisable` fires, since the two aren't in the same teardown chain — this caused a real `NullReferenceException` in `GoldDisplayUI.OnDisable` during testing.

### N. Soldier (implemented)
`Assets/02. Script/Soldier/` — auxiliary attackers that fight alongside the Player, actively seek targets (no longer stationary), can be hit and killed, and respawn:
- `SoldierSpawner` (MonoBehaviour) — on `Start()`, `PoolManager.EnsurePool`s each entry's prefab, spawns one at each `SoldierSpawnEntry.SpawnPoint`, and registers it with a `SoldierRespawner` it owns. No Rank-gating yet (Rank system isn't implemented) — soldiers always exist once the spawner's GameObject is active.
- `SoldierSpawnEntry` (`[Serializable]`, not a SO) — one spawn slot: `SoldierPrefab`, `SpawnPoint` (lets different points use different prefabs, e.g. melee vs. ranged).
- `SoldierRespawner` (plain C# class, `ITickable`, owned/constructed by `SoldierSpawner`) — subscribes `CharacterDiedEvent`; if the dead GameObject is one it's tracking (`Dictionary<GameObject, SoldierSpawnEntry>`, not tag/string based), starts a `respawnDelay` (default 5s) countdown, then re-spawns at the same `SpawnPoint` via `PoolManager.Get` and re-registers it. Ticks down via `GameTicker`, not a coroutine.
- `Soldier.prefab`/`Soldier_Ranged.prefab` — reuse existing `Character`/`Combat` components by composition, same as Player/Monster: `CharacterStatsProvider`, `Health`, `CharacterMover`, `EnemyTracker` + `SoldierEnemyFilter` (below) for actively finding and walking to a target, `Attacker` (targets the Monster layer) + `MeleeAttackBehavior`/`RangedAttackBehavior`, `CircleCollider2D` (non-trigger, same setup as Player's — needed so `Monster.ContactAttacker`'s trigger scan can actually detect and damage soldiers; without it soldiers were untouchable by construction), `PoolReleaseOnDeath` (soldiers weren't pooled-and-released on death before — now they are, which is what makes `SoldierRespawner` able to reuse the instance).
- `SoldierTargetRegistry` (`IManager`/`IService`) — `Dictionary<GameObject soldier, Health target>` of which monster each soldier currently claims. `IsClaimedByOther(candidate, claimant)` and `SetClaim` support `SoldierEnemyFilter`'s spreading logic; `TryGetClaimant(target, out claimant)` is the reverse lookup `Combat.MonsterTargetSelector` uses to find "which soldier is targeting me."
- `SoldierEnemyFilter` (`ITargetFilter`, on Soldier prefabs) — makes `EnemyTracker` prefer monsters no *other* soldier has already claimed (falls back to nearest-any if every candidate is claimed), so soldiers spread across multiple monsters instead of dogpiling one. Also subscribes `CharacterDiedEvent` for itself and clears its own claim immediately on death, so `MonsterTargetSelector` doesn't keep steering a monster at a soldier that's already been pooled/released.

See Combat section for `EnemyTracker`/`ITargetFilter`/`MonsterTargetSelector` — the priority system that decides whether a monster goes after the Player or a claiming Soldier.

### O. Save & Persistence (implemented)
`Assets/02. Script/Save/` — the only system that touches `PlayerPrefs`; every other system reads/writes save state purely by publishing/subscribing events, never by calling `SaveService` directly except to `Load()` an initial snapshot at startup:
- `SaveData` (`readonly struct`) — `Gold`, `EnhancementStones`, `Chapter`/`StageNumber` (current/resume stage), `HighestClearedChapter`/`HighestClearedStageNumber` (best-ever record, see Stage Progression section), `LastActiveUnixTime`, `AttackPowerLevel`/`MaxHealthLevel` (stat enhancement levels), `InventoryJson` (owned equipment + equipped slots, see below).
- `SaveService` (`IManager`/`IService`) — subscribes `GoldChangedEvent`, `Equipment.Events.EnhancementStoneChangedEvent`, `Stage.Events.StageChangedEvent`, `Stage.Events.HighestStageClearedEvent`, `Enhancement.Events.StatEnhancedEvent`, `Inventory.Events.InventoryChangedEvent`, `Inventory.Events.EquipmentEquippedEvent`; any one of them updates its internal fields and immediately writes the *entire* `SaveData` to `PlayerPrefs` (not just the changed field — simplest way to guarantee nothing drifts out of sync). `Load()` returns sane defaults (`0` for gold/stones/highest-cleared/enhancement levels, `1` for current chapter/stage, `""` for `InventoryJson`) when there's no prior save.
- **Gotcha (seed before subscribe):** `SaveService.Initialize()` must call `Load()` and copy every field into its own internal state *before* subscribing to any event. Its internal fields otherwise start at C# defaults and only get populated reactively by their own first event — so if a *different* tracked event fires first and triggers `Save()`, it writes those never-yet-populated fields back as defaults and **overwrites real saved data with zeroes**. This actually happened: `EnhancementService.RestoreLevel()` (below) published `StatEnhancedEvent` before `OfflineProgressService` had published a fresh `StageChangedEvent`/`HighestStageClearedEvent`, and when the offline-progress elapsed-time budget was `<= 0` (e.g. rapid Editor stop/play within the same second) it skipped its own corrective publish — permanently resetting stage/highest-cleared progress to defaults. Fixed by seeding from `Load()` first; keep this in mind before adding any new tracked event.
- `Stage.Events.HighestStageClearedEvent` — separate from `StageChangedEvent` on purpose: it only fires when a stage becomes a **new** best-ever record, whereas `StageChangedEvent` fires on every stage load (advance, repeat, or death fallback). Keeping them separate is what lets "current position" and "best-ever position" diverge and both still persist correctly.
- **Stat enhancement levels** — `EnhancementService.RestoreLevel(statType, level)` sets the level directly (no gold spent) and republishes `StatEnhancedEvent` with the *cumulative* bonus (`ValuePerLevel * level`) so `Character.StatEnhancementReceiver` applies it in one shot. Called from `GameBootstrapper.Start()` (after every object's `OnEnable`, same timing reason as `OfflineProgressService.CalculateAndApply()` — `StatEnhancementReceiver` must already be subscribed or it misses the event).
- **Inventory/equipped snapshot** — a variable-length collection can't be flat `SaveData` fields, so it's serialized as one JSON blob (`JsonUtility`) keyed by `EquipmentCatalogSO` index rather than by `EquipmentSO` reference (PlayerPrefs can't hold asset references). `EquipmentCatalogSO.IndexOf`/`GetAt` added to match the other index-based catalogs. `InventoryService.ExportSnapshot`/`RestoreSnapshot` and `EquippedGearService.ExportSnapshot`/`RestoreSnapshot` do the actual (de)serialization; neither publishes events when restoring (it's seeding, not gameplay). `SaveService` takes `InventoryService`/`EquippedGearService`/`EquipmentCatalogSO` as constructor dependencies — the same *composed direct dependency* shape as `EnhancementService` depending on `CurrencyService` — because rebuilding a full snapshot on every change can't be done from event payloads alone (`InventoryChangedEvent`/`EquipmentEquippedEvent` only say "something changed", not the full state). `GameBootstrapper.Awake()` constructs `InventoryService`/`EquippedGearService` *before* `SaveService` so they're available to inject, then calls `saveService.RestoreInventory(save)` right after the initial `Load()` (no `OnEnable`-timing concern here, since `EquipmentPanelUI`/`EquippedSlotBarUI`/`EquipmentSlotPopupUI` all re-query current state on their own `OnEnable`/`Open()` rather than relying on catching a one-shot event).

### P. Offline Progress (implemented)
`Assets/02. Script/Offline/` — approximates what would have happened while the app was closed; explicitly does **not** simulate player death or stage failure:
- `OfflineProgressService` — `CalculateAndApply()` runs from `GameBootstrapper.Start()` (after other objects' `OnEnable`/event subscriptions, so `OfflineProgressCalculatedEvent` isn't missed by the popup UI). Budget = `min(elapsed since LastActiveUnixTime, maxOfflineHours)`. Simulation now starts from **`HighestCleared`**, not the saved current/resume stage — offline time represents best-case play with no death risk, so it shouldn't be held back by a temporary death-fallback the player was sitting in when they left. If it reaches the last stage in the catalog, it keeps repeating that stage for the rest of the budget instead of stopping (mirrors the real-time repeat behavior). Uses `LootRoller` for identical drop odds to real combat.
- At the end, publishes `StageChangedEvent` (new resume point) and, if the record advanced, `HighestStageClearedEvent` — so `StageController.Start()`'s `SaveService.Load()` (which runs right after) picks up the post-offline state, and the player returns to a breakthrough frontier rather than wherever they'd fallen back to before going offline.

### Q. Stage Progression: Breakthrough & Repeat (implemented)
`Stage/StageProgression.cs` — the policy layer above `StageController`/`StageCatalogSO` implementing: normally advance through new stages ("돌파"); once there's nothing left to advance into (or the player died attempting new ground), fall back to repeating a stage ("반복"); repeat-clearing climbs back up one stage at a time until breakthrough naturally resumes at whichever stage was originally failed. Death always regresses, but never past `highest − maxRegressionDistance`.
- State is exactly two catalog **indices** (not `(chapter, stageNumber)` pairs — indices make "next"/"one back"/"cap distance" trivial arithmetic via `StageCatalogSO.IndexOf`/`GetAt`): `_currentIndex` (where you're playing) and `_highestClearedIndex` (your record, never regresses). **There is no separate persisted "mode" field** — Breakthrough vs. Repeat is a pure function of comparing the two (`current == highest + 1` → breakthrough/new ground; `current <= highest` → repeating already-cleared ground) — so a saved mode can never drift out of sync with the positions.
- On `StageClearedEvent`: if `current > highest` (new ground), `highest` advances to `current` (publishes `HighestStageClearedEvent`), then `current` moves to the next stage if one exists, else stays put (repeat the last stage forever). If `current <= highest` (repeat-clearing known ground), `current` just steps forward by exactly one — repeated clears climb back toward `highest + 1` one at a time, and once `current` reaches `highest + 1` and clears it, that's a new record and breakthrough resumes automatically at the stage the player originally died on.
- On `CharacterDiedEvent` (filtered to the Player via a `Transform` reference, the same filtering pattern `StageProgressTracker` already uses for monsters): regardless of current mode, `current` steps back exactly one stage, clamped to `max(current − 1, highest − maxRegressionDistance, 0)`. `highest` never regresses on death.
- `StageController` passes `playerTransform`, `maxRegressionDistance` (`[SerializeField]`, default 20), and the initial current/highest `StageSO`s (resolved from `SaveData` at `Start()`) into `StageProgression`'s constructor.

### R. Stage Difficulty Scaling & Monster Tiers (implemented)
`Character/StageMonsterScaler.cs` + `Stage/StageDifficultyConfigSO.cs` — monster stats scale automatically per stage instead of needing a unique stat SO per stage:
- `StageDifficultyConfigSO` — one shared data asset: `baseMultiplier`(1) + `multiplierPerStageIndex`(0.15) → `GetMultiplier(stageIndex)` is a plain linear formula. No per-stage authoring needed as new stages are added.
- `StageMonsterScaler` (Character domain, Monster-only) — `ApplyScale(multiplier)` recomputes `MaxHealth`/`AttackPower` from `CharacterStatsProvider.BaseStats` (the original SO, not the current `RuntimeStats`) so scaling never compounds across pool reuse, then calls `Health.Revive()` to refill current HP to the new max.
- `StageController.LoadStage` computes `catalog.IndexOf(stage)` → `StageDifficultyConfigSO.GetMultiplier` → passes the multiplier into `MonsterSpawner`'s constructor; `MonsterSpawner.SpawnOne` calls `StageMonsterScaler.ApplyScale()` on each freshly spawned instance (same spot/pattern as the existing `MonsterTargetSelector.Initialize()` call).
- Monster tiers: `Monster.prefab`(일반, reused as-is) + `Monster_Elite.prefab` + `Monster_Boss.prefab` — each is a duplicate of `Monster.prefab` with a different `CharacterStatsProvider.baseStats`/`MonsterLootProvider.loot` pair only (same sprite/visual, no new art).
- Content authored (superseded — see section V): originally just `Stage_1_1`~`Stage_1_10`; now the full 8-chapter/40-stage catalog described in section V.

### S. Equipment Stats on Wear (implemented)
`Assets/02. Script/Equipment/` + `Assets/02. Script/Character/` — equipped gear now actually contributes stats instead of being purely cosmetic:
- `EquipmentStatConfigSO` — data asset: `SlotStatEntry[]` (`EquipmentType Slot`, `EnhancementStatType StatType`, `BaseValue`, `PerGradeIndex`). Slot→stat mapping is data-driven (not hardcoded per-slot in code) — `GetEntries(slot)` returns *all* matching entries, so one slot can contribute multiple stats just by adding more rows with the same `Slot`, no code change needed.
- `EquipmentStatMath.CalculateBonus(entry, gradeIndex, enhancementLevel, statBonusPerLevel)` — pure function: `(BaseValue + PerGradeIndex * gradeIndex) * (1 + statBonusPerLevel * enhancementLevel)`. Shared by `EquipmentStatService`'s real equipped-total calculation and the equipment detail popup's arbitrary-item preview, so the two never drift apart.
- `EquipmentStatService` (`IManager`/`IService`) — subscribes `EquipmentEquippedEvent` and `InventoryChangedEvent` (covers enhancing an already-equipped line), recomputes totals per `EnhancementStatType` across all 5 slots, and publishes `Equipment.Events.EquipmentStatsChangedEvent(StatType, NewTotalBonus)` only for stats whose total actually changed. `CalculatePreview(definition, enhancementLevel)` exposes the same math for any item regardless of equip state (used by the detail popup, see section T).
- `Character.RuntimeStatApplier` — single `Dictionary<EnhancementStatType, Action<RuntimeStats, float>>` mapping table shared by both `StatEnhancementReceiver`(강화) and `EquipmentStatReceiver`(장비), so adding a future stat (attack speed, move speed, ...) only means one new enum value + one new entry in this table, not edits scattered across receivers.
- `Character.EquipmentStatReceiver` (Player only) — subscribes `EquipmentStatsChangedEvent`. Unlike enhancement (monotonic, safe to `+=`), equipping can *decrease* a stat (unequip/swap), so this receiver tracks its own previously-applied total per stat and applies only the delta to `RuntimeStats`.
- **Gotcha (stale equip reference silently kept applying stats)**: equipping doesn't consume `Count` (`EquippedGearService.Equip` just points at a stack), so fully consuming that stack as fusion material (`EquipmentFusionService` → `InventoryService.TryConsume`) used to leave `EquippedGearService` pointing at an `OwnedEquipment` no longer in `InventoryService` — silently keeping its stat bonus applied forever. Fixed: `EquippedGearService` now also subscribes `InventoryChangedEvent`; `InventoryService.TryConsume` publishes the event with the same object's `Count` already at 0 right before removing the line, so `evt.Changed.Count <= 0` on the currently-equipped instance is the exact signal to auto-unequip that slot.
- Save: no dedicated save state — equipment stats are *derived* every time from `InventoryService`/`EquippedGearService`'s own persisted state (see Save section), recomputed via `EquipmentStatService.RecomputeAndPublish()` called once from `GameBootstrapper.Start()` after `RestoreInventory()` (which itself doesn't publish events).
- Content: `EquipmentStatConfig.asset` currently has 5 placeholder entries (Weapon→AttackPower, other 4 slots→MaxHealth) — balance numbers, not final.

### T. Equipment Detail/Comparison Popup (implemented)
`Assets/02. Script/UI/` — lets a player inspect any owned item's stat options, not just the currently equipped one:
- `EquipmentRowUI` — the item-name `Text` now sits on its own `Button` (`nameButton`, a separate GameObject/component from the row's own `rowButton`) so tapping the name doesn't trigger equip; `Initialize(...)` takes an optional `Action<OwnedEquipment> onDetailRequested` (default `null`, existing callers unaffected).
- `EquipmentDetailPopupUI` — `Open(target, currentlyEquipped)` shows `target`'s options (via `EquipmentStatService.CalculatePreview`) and, if `currentlyEquipped` is a different item in the same slot, a per-stat ▲/▼ diff against it (same slot ⇒ same stat set, no dictionary merging needed). `Close()` is public for the same "parent popup closes me too" reason as `EquipmentSlotPopupUI.Close()`.
- `EquipmentSlotPopupUI` now holds a `detailPopup` reference: wires each row's `onDetailRequested` to `detailPopup.Open(...)`, and its own `Close()` also closes the detail popup (same nested-popup lifecycle pattern as `EquippedSlotBarUI` → `EquipmentSlotPopupUI`).
- `UI.StatDisplayNames` — the `EnhancementStatType → 한글 이름` switch used to live only in `StatRowUI`; pulled out to a shared static helper so `EquipmentDetailPopupUI` doesn't duplicate it (same "single edit point for future stats" motivation as `RuntimeStatApplier`).

### U. Rank System (implemented)
`Assets/02. Script/Rank/` — 시골 소년 → 병사 → ... progression gated by stage milestones, per spec section 2.B:
- `RankSO` — `DisplayName` + `RequiredStage` (a `StageSO` reference; `null` = starting rank, no condition).
- `RankCatalogSO` — ordered `RankSO[]`, same `IndexOf`/`GetAt`/`GetNext` shape as every other catalog.
- `RankService` (`IManager`/`IService`) — subscribes `Stage.Events.HighestStageClearedEvent` (fires only on a *new* best-ever record — real-time progression and `OfflineProgressService` both funnel through it). On each event, promotes through as many ranks as the new record satisfies in one pass (`while` loop), since offline progress can jump straight past several milestones without an event per intermediate stage. `IsAtLeast(rank)` (`rank == null` ⇒ always true) is the query other systems use to gate themselves.
- `Rank.Events.RankChangedEvent` — carries `NewRank`, `NewRankIndex`, and `IsRestore` (true when published by `RestoreLevel`/`CatchUpFromHighestStage` at startup, false for a genuine new promotion) — lets subscribers like the promotion popup ignore the "just loaded the save" case.
- `RankService.RestoreLevel(rankIndex)` mirrors `EnhancementService.RestoreLevel` (called from `GameBootstrapper.Start()`, same timing). `RankService.CatchUpFromHighestStage(chapter, stageNumber)` is called right after — **gotcha this fixed**: a save whose `HighestClearedStageNumber` already exceeded a rank's requirement *before* the Rank system existed would otherwise never promote, since `RankService` only reacts to *new* record events, not the one already sitting in the save. This silently (no popup) re-derives any promotions the saved record already earned.
- Save: `SaveData.RankIndex`, persisted the same way as the other levels.
- `Soldier.SoldierSpawner` — gained an optional `requiredRank` field (`null` = always active, preserves old behavior). Spawn logic moved into `TrySpawn()`, called from `Start()` and again on every `RankChangedEvent`, guarded by a `_spawned` flag so it only actually spawns once `RankService.IsAtLeast(requiredRank)` is true — fulfills the gating this component's doc comment had anticipated since the Soldier section was first written.
- `UI.RankUpPopupUI` — same shape as `OfflineProgressPopupUI`; shows "랭크 승급!" + the new rank's name, skips any event with `IsRestore == true`. Stands in for the spec's "Short Dialogue Cutscene" — no Dialogue/Cutscene system exists yet, this is a deliberately lighter placeholder.
- Content: `RankCatalog.asset` currently has 2 entries (시골 소년 start, 병사 requires `Stage_1_10`) — placeholder, meant to grow alongside stage content (e.g., one rank per chapter clear).

### V. Stage Content: 1-1 ~ 8-40 (implemented)
- Stage content now spans all 8 chapters × 40 stages (320 `StageSO` assets total; only 1-1~1-10 were hand-authored, the remaining 310 were generated). Monster wave composition follows one formula across every chapter (chapter-relative stage `s` = 1..40): normal-monster count 15→30 and interval 1.0→0.5s linearly across the chapter; elite wave added from `s=20` onward, count 3→20 and interval 1.5→0.8s linearly; boss wave (1, interval 2.0s) added only on `s=40`. Monster *stats* are still scaled separately and automatically by `StageDifficultyConfigSO` (per catalog index) — this formula only tunes wave pacing/composition.
- Equipment drops start at chapter 6 as originally designed (chapters 1-5 stay empty). Chapters 6/7/8 each own exactly one grade tier (커먼/언커먼/레어 respectively), advancing one grade every 8 stages within the chapter; every stage in 6-8 offers all 5 slots at that stage's grade, 8% drop chance each.
- `StageCatalog.stages` is a flat, fully-ordered 320-entry array (chapter 1 stages 1-40, then chapter 2, ... chapter 8) — `StageProgression`'s index arithmetic depends on this exact ordering.
- These are placeholder pacing/balance numbers (same spirit as `EquipmentStatConfig`'s placeholders), generated via a one-off Editor script rather than 310 hand-authored assets — safe to hand-tune individual `StageSO` assets afterward without touching the generation code.

### W. Soldier Behavior: Off-Screen Return Priority & Ranged Kiting (implemented)
`Assets/02. Script/Combat/CameraVisibility.cs` + `Assets/02. Script/Soldier/SoldierBehaviorController.cs` — soldiers now prioritize getting back on-screen over any combat logic, and ranged soldiers kite instead of tanking hits at melee range:
- `CameraVisibility.IsOnScreen(camera, worldPosition, viewportMargin = 0f)` — static helper extracted from `EnemyTracker`'s original on-screen check, so target-candidate filtering and a soldier's own off-screen check share one viewport-margin-aware formula.
- `SoldierBehaviorController.Evaluate()` checks `CameraVisibility.IsOnScreen` on its *own* position first, before consulting its `BehaviorProfileSO` rules at all — if off-screen, it skips straight to `ReturnToScreen()` (disables `EnemyTracker`, points `CharacterMover.Target` at a viewport-clamped position) regardless of engage/hold/retreat mode.
- **Gotcha:** the return target must be a fresh, *unparented* scratch `Transform` (`_returnAnchor`, created once in `Awake()`, repositioned every call). An earlier attempt parented it to the soldier itself — since a child's world position tracks its parent, the anchor receded at exactly the rate the soldier walked toward it, so the soldier ran in a straight line forever. Sending it to the deployment slot's own spawn point (the original idea) doesn't work either: those sit below the camera's visible area by design (an off-screen staging line), so "return to spawn point" would just send it to *another* off-screen point forever.
- `Combat.Attacker.AttackPerformed` (`event Action`) — fires right after `_attackBehavior.Execute(...)`. A same-GameObject notification, not `EventBus` (Soldier already composes Combat components directly), that `SoldierBehaviorController` subscribes to for ranged-kiting timing.
- Ranged kiting (only when `RangedAttackBehavior` is present on the same soldier) replaces `EnemyTracker` entirely for that unit while `Engage`: on `AttackPerformed`, starts a `postAttackLockDuration` (default 0.5s) root (`CharacterMover.Target = null`). Once the lock expires, if the nearest on-screen enemy is within `AttackRange`, it retreats — tries 5 candidate directions (straight away from the enemy, then that vector rotated ±45°/±90°) at `kiteStepDistance`, keeping only ones that stay inside the viewport (`kiteScreenMargin`) and picking whichever maximizes distance from the enemy. If every candidate would leave the screen (cornered), it holds position instead of forcing itself off-screen, since that would just re-trigger the off-screen-return priority above.

### X. Monster Targeting Simplified to Nearest-Ally, Real Attack Range, Visual Tiering (implemented — supersedes the `MonsterTargetSelector` description in section F)
`Assets/02. Script/Combat/MonsterTargetSelector.cs` — replaced the old claim-priority chain (player-if-currently-targeted, else claiming soldier, else player fallback) with a plain nearest-target scan:
- `ChooseTarget()` runs `Physics2D.OverlapCircleAll(position, detectionRange, allyLayerMask)` and returns the nearest live `Health`, falling back to `PlayerTransform` if nothing is in range — the same nearest-wins idiom `EnemyTracker` already used for the opposite faction.
- `allyLayerMask` points at the `Player` layer; `Soldier`/`Soldier_Ranged` prefabs were moved onto that layer (previously `Default`) so one scan naturally covers both the Player and any Soldier.
- `Combat.PlayerTargetTracker`/`Combat.PlayerTargetMarker` and `Soldier.SoldierTargetRegistry.TryGetClaimant` lost their only consumer (the removed priority chain) and were deleted. `SoldierTargetRegistry.IsClaimedByOther`/`SetClaim` remain — still used by `SoldierEnemyFilter` for soldier-vs-soldier target spreading, an unrelated concern.
- Monster's own attack switched from `ContactAttacker` (pure physics-trigger, no range concept at all) to the same `Attacker` + `MeleeAttackBehavior` composition Player/Soldier use, targeting the `Player` layer. `MonsterTargetSelector.Retarget()` now also sets `CharacterMover.StoppingDistance = Stats.AttackRange` (mirroring `EnemyTracker`), so monsters stop at range instead of walking fully onto the target's position — `StoppingDistance` previously defaulted to `0`, which is why monsters used to visually stack exactly on top of whatever they were attacking. All three monster tiers' `CharacterStatsSO.AttackRange` set to `2.5` (matching melee `Soldier`) — previously `0` and silently unused since `ContactAttacker` never read it.
- `Monster_Elite.prefab`/`Monster_Boss.prefab` also get a purely visual `transform.localScale` bump (1.2x / 1.5x) so they read as tougher at a glance. `AttackRange` lives on `CharacterStatsSO`, entirely independent of transform scale, so this doesn't touch combat reach. Each prefab's `HealthBar` child is counter-scaled (`1/scale`) so the bar itself stays a consistent on-screen size instead of growing with the body.

### Y. Rank Gating Fixes: Startup Promotion Replay & Locked Soldier Tab (implemented — supersedes part of section U)
- **Gotcha fixed:** `RankService` used to sit at its constructor-default rank (index 0) until `GameBootstrapper.Start()` called `RestoreLevel`. But `OfflineProgressService.CalculateAndApply()` runs *before* that in `Start()`, and if simulated offline progress advanced the record, `RankService.OnHighestStageCleared` re-derived every rank from scratch as if newly earned (`isRestore: false`) — replaying the rank-up popup on every single launch for ranks the player had already reached long ago. Fixed with `RankService.SeedRank(rankIndex)`, which sets the in-memory rank silently (no event) from the save; called in `GameBootstrapper.Awake()` right after `SaveService.Load()`, strictly before offline progress ever runs.
- `UI.SoldierPanelGateUI` (on the `SoldierPanel` bottom-tab panel) — while the current rank is below a configured `requiredRank`, shows a locked message instead of the roster/deployment lists (which used to render as unexplained empty containers). The message is built from `requiredRank.RequiredStage` (`"{chapter}-{stageNumber} 스테이지 클리어 후 오픈됩니다."`), so it tracks whatever the real requirement is with no code change if the rank/stage is retuned. Subscribes `RankChangedEvent` so reaching the rank while the tab is already open unlocks it immediately.

### Z. Soldier Deployment & Equipment Fixes (implemented)
- `Soldier.SoldierDeploymentService.TryAssign` — a soldier can now occupy at most one deployment slot at a time. Assigning an instance that's already deployed elsewhere auto-vacates the old slot first; the deployment picker popup (`SoldierDeploymentPickerPopupUI`) also excludes units already deployed to a *different* slot from the pick list. Previously nothing enforced this, so a single soldier instance could be assigned to every slot at once (no real exclusivity).
- `SoldierEquippedGearService.Equip` now actually **consumes** 1 unit from `SoldierEquipmentInventoryService`'s stock instead of just pointing at the stack (the model it originally copied from the player's own `Inventory.EquippedGearService`, which works there because the player has exactly one wearer). With many soldiers able to want the same line, the old non-consuming model let every soldier "equip" the same single copy at once. `Equip` now fails if stock is empty, is a no-op if the unit already has that exact line equipped, and returns a swapped-out item's copy to stock. A new `Unequip` (previously nonexistent — equipping was one-way) also returns the copy to stock. The old "count hits zero → auto-unequip everyone on that line" safety net (originally needed because equipping never touched count) was removed, since it now misfires on the *expected* case of equipping the last copy.

### AA. Gacha Popup: Categories, Tiers & Multi-Pull (implemented)
`Assets/02. Script/Gacha/` + `Assets/02. Script/UI/` — a full-screen modal gacha screen, replacing the inline pull button that used to live in the soldier roster panel:
- The bottom tab bar's fifth slot (previously "유물"/`RelicPanel`, an unimplemented placeholder) was repurposed into "뽑기", wired directly to `UI.GachaPopupUI.Open()` rather than through `BottomMenuUI`'s normal panel array — this popup needs to cover the tab bar itself, which the other four tabs don't.
- Two-level tab structure, built by nesting the existing generic `BottomMenuUI` shell twice: an outer category row (무기 뽑기 / 병사 뽑기) and, per category, an inner tier row (일반 / 고급 / 유료). Adding either a new category or a new tier is just one more button+panel entry in an inspector array — no new tab-switching code. Only the "일반" tier is wired to a working table on either side; 고급/유료 are placeholder-only ("준비 중") since neither a probability table nor a currency source exists for them yet.
- `EquipmentGachaPoolEntry`/`EquipmentGachaTableSO`/`EquipmentGachaService` mirror the pre-existing `GachaPoolEntry`/`GachaTableSO`/`GachaService` (soldier gacha) structure exactly, but spend gold instead of soldier tickets and grant the result by republishing `Loot.Events.ItemDroppedEvent` (the same acquisition path `EquipmentFusionService` already reuses) rather than touching `InventoryService` directly.
- Both services expose `Pull(tierIndex, count)`: loops up to `count` times, stopping the moment currency runs out (partial success — a "300개 뽑기" with only enough currency for 50 still nets 50, not zero), and publishes one aggregated `SoldierPulledEvent`/`EquipmentPulledEvent` (now carrying `IReadOnlyList<T>` instead of a single item) rather than one event per pull. A single "1개 뽑기" is just `count = 1` through the same path. `SoldierPulledPopupUI`/`EquipmentPulledPopupUI` group the batch by item name and show per-item counts.
- **Gotcha fixed:** `SoldierPulledPopup` predates `GachaPopup` in the Canvas hierarchy, so its result notification rendered *behind* the still-open gacha popup (later siblings draw on top). Moved to the last sibling.

### AB. UI Layout & Discoverability Fixes (implemented)
- **Equipment row had no discoverable way to equip.** The row's biggest, most natural tap target (the item name) opens a read-only stat detail popup with no equip action, and the actual equip trigger was the row's own background `Button` sitting *behind* the name/fuse/enhance children — which, via `HorizontalLayoutGroup`, tile the row's entire width, leaving only a ~20px sliver of uncovered padding clickable. Added an explicit "장착" button (same pattern as the existing fuse/enhance buttons), disabled and reading "장착됨" once that line is the one currently worn.
- **Player was missing a head HP bar.** Monster/Soldier prefabs already nest `HealthBar.prefab`; Player didn't. Added the same instance at the same local offset.
- **`EquippedSlotBarUI` slots showed nothing when empty.** Each slot only ever displayed the equipped item's name (or `"-"`), so an empty slot gave no clue which gear type it represented. Added a permanent slot-type label (무기/장갑/갑옷/투구/신발) above the existing item-name label — purely static scene content, no code change, since the slot type never changes at runtime.
- **List popups with no scroll overflowed their frame.** `EquipmentSlotPopupUI`'s row container, `SoldierDeploymentPickerPopupUI`'s row container, and `StatPanel`'s row list all either rendered past their popup/panel bounds (no `RectMask2D`) or had `sizeDelta` values that canceled out to a literal zero-height container against their parent's own size (so rows existed but nothing was visible). All three were rebuilt into the same `RowContainer(ScrollRect) → Viewport(RectMask2D) → Content(VerticalLayoutGroup + ContentSizeFitter)` structure, which is now the standard pattern for any scrollable list in this project.

### AC. Enhancement System Expansion: New Stats & Critical Hits (implemented)
`Assets/02. Script/Enhancement/` + `Assets/02. Script/Character/` + `Assets/02. Script/Combat/Attacker.cs`:
- `EnhancementStatType` gained `AttackSpeed`, `MoveSpeed`, `CriticalChance`, `CriticalDamage` (supersedes the two-value enum shown in section L). `CharacterStatsSO`/`RuntimeStats` gained matching `CriticalChance` (default `0`) and `CriticalDamageMultiplier` (default `0.5` — a critical hit deals `AttackPower * (1 + CriticalDamageMultiplier)`, so the default is "crits deal 150%").
- `Combat.Attacker` rolls `Random.value < Stats.CriticalChance` on every attack and scales damage accordingly before calling `IAttackBehavior.Execute`. This applies to whatever shares the component — Player, Soldier, and Monster alike — but monsters never crit in practice since their base `CriticalChance` is `0`.
- `Character.RuntimeStatApplier` now takes the owning character's *base* `CharacterStatsSO` alongside its `RuntimeStats`, because `AttackSpeed`/`MoveSpeed` are designed as "N% of the character's own base value per level" rather than a flat delta (e.g. `stats.MoveSpeed += baseStats.MoveSpeed * delta`) — `CriticalChance`/`CriticalDamage` stay flat additive (percentage-point) since they have no meaningful "base" to scale from. Both `StatEnhancementReceiver` and `EquipmentStatReceiver` (which share this mapping table) pass `CharacterStatsProvider.BaseStats` into the call now.
- `EnhancementConfigSO.CostIncreasePerLevel` (linear, `int`) was replaced with `CostMultiplier` (compound, `float`) — next cost is `BaseCost * CostMultiplier ^ level`, computed in `double` and saturated at `int.MaxValue` rather than cast straight to `int`. **Gotcha this fixed:** the naive `int` cast overflowed (wrapped negative) around level 47 at a 1.5x multiplier, and `CurrencyService.TrySpendGold` then read the negative cost as "affordable," making enhancement free (and net gold-*positive*, since spending a negative amount is adding). Current tuned values: `AttackPower`/`MaxHealth`/`AttackSpeed`/`MoveSpeed` at 1.05x–1.1x (`AttackPower`/`MaxHealth` 1.05x, `AttackSpeed`/`MoveSpeed` 1.1x), `CriticalChance`/`CriticalDamage` at 2.1x, all with `MaxLevel = 2000` (previously 50) — chosen so the compound curve stays affordable across a meaningfully large number of levels instead of most of the level range being purely decorative, since monster HP scaling (section X derived from section R) grows *linearly* with stage index while a much steeper compound rate does not correspond to that at all.
- `AttackSpeed`/`MoveSpeed`: 1%/level, capped at level 20 (±20% off base). `CriticalChance`: 0.1%/level to level 500. `CriticalDamage`: 0.5%/level to level 300 (i.e. the crit bonus fraction, not total multiplier — see above).
- `UI.StatDisplayNames.FormatValue(statType, value)` — new formatting helper so the four percentage-based stats render as `"N%"` in `StatRowUI` instead of a raw decimal (`0.01` read as meaningless before this).
- `Save.SaveData`/`SaveService` gained one level field per new stat, following the exact same per-stat-field pattern as `AttackPowerLevel`/`MaxHealthLevel` (no generic collection — six named int fields, consistent with existing style rather than a new serialization scheme).

### AD. Offline Progress: Difficulty-Accurate & Repeat-Only (implemented — supersedes section P)
`Assets/02. Script/Offline/OfflineProgressService.cs`:
- **Gotcha fixed:** the offline combat simulation computed monster health from each monster's flat `CharacterStatsSO.MaxHealth`, completely ignoring `StageDifficultyConfigSO`'s per-stage-index multiplier that real combat applies via `StageMonsterScaler` (up to ~49x by the final stage). The simulation was effectively always fighting stage-1-strength monsters, so a long-enough offline window could blow through most of the 320-stage catalog in one sitting — including stages the player could never actually clear live. Fixed by computing `_difficultyConfig.GetMultiplier(_catalog.IndexOf(stage))` and folding it into the simulated monster's health exactly like `StageMonsterScaler` does.
- Even with health scaling fixed, the simulation still advanced into new, unproven stages every time it "cleared" one. Redesigned to be repeat-only: it now simulates the player's *highest-cleared* stage on repeat for the entire time budget and never advances the record or resume position past it, always resuming there afterward regardless of whatever death-regressed position (see `StageProgression`, section Q) was current when the player left. `HighestStageClearedEvent` is no longer published from here at all, since the record never changes.
- `Offline.Events.OfflineProgressCalculatedEvent.StagesCleared` renamed to `TimesCleared`, reflecting that it now counts repeat-clears of one stage rather than newly-conquered stages.

### AE. War Objective Progress & Pre-Climax Warmup Warning (implemented)
`Assets/02. Script/War/` + `Assets/02. Script/UI/` — adds a readable progress signal to the four War objectives (section context: `WarObjectiveType` = `Annihilation`/`StructureCapture`/`BossDefeat`/`CargoProtection`) and a 3-second warning countdown before a climax fight's judgment actually starts:
- `IWarObjective.Progress01` (0~1) — `AnnihilationObjective`/`BossDefeatObjective` just return `IsCompleted` as 0/1 (no continuous progress exists); `CargoProtectionObjective` returns `elapsed / protectDuration`; `StructureCaptureObjective` averages `WarStructure.Control` across `requiredStructures`.
- `WarBattleController` no longer activates the objective the instant a climax stage (`StageNumber == climaxStageNumber`) loads. It now starts a `climaxWarmupDuration` (default 3s) countdown, publishing `War.Events.WarClimaxWarmupStartedEvent(objectiveType, chapter, duration)` instead. `Tick()` counts the warmup down and, once it hits zero, runs the previous immediate-activation logic (`ActivateObjective`, `_isActive = true`, publish `WarClimaxStateChangedEvent(true, ...)`). `warZoneRoot` (structures/cargo) still activates immediately so the battlefield is visible during the countdown, but `ResetForNewAttempt()` — and therefore judgment — only runs once warmup ends, so any `WarStructure` progress ticked during the countdown is wiped before real judgment starts. Leaving climax (or re-entering) mid-warmup resets `_isWarmingUp` cleanly.
- `War.Events.WarClimaxStateChangedEvent(IsClimax, ObjectiveType, Chapter)` — fires when the real fight starts (`IsClimax=true`, after warmup) and when climax is left (`IsClimax=false`). `War.Events.WarObjectiveProgressChangedEvent(Progress01)` — fires from `Tick()` only while the objective is actually active, whenever `Progress01` changes.
- `UI.WarBattleHudUI` — subscribes `WarClimaxStateChangedEvent`/`WarObjectiveProgressChangedEvent`; shows an objective banner always, and a progress gauge only for `StructureCapture`/`CargoProtection` (`Annihilation` already has `StageInfoUI`'s remaining-monster count as an equivalent signal, `BossDefeat` has the boss's own `HealthBarUI` — see gotcha below for why that pairing needed a follow-up fix).
- `UI.WarClimaxWarningUI` — subscribes `WarClimaxWarmupStartedEvent` (show banner + a locally-ticking countdown number) and `WarClimaxStateChangedEvent` (hide unconditionally, regardless of `IsClimax` — covers both "real fight just started" and "player left climax mid-warmup" with one rule).
- `UI.WarObjectiveBannerText` — static `WarObjectiveType → 한글 배너 문구` helper shared by `WarBattleHudUI`/`WarClimaxWarningUI` so the two don't duplicate the switch (same motivation as `StatDisplayNames`/`RuntimeStatApplier`).
- **Gotcha fixed:** `StageInfoUI` showed "남은 몬스터: X/Y" unconditionally on every stage, including climax stages running a non-`Annihilation` objective — where remaining monster count isn't the real clear condition and just confuses the player (e.g. during `StructureCapture`, monsters can keep spawning/dying with zero relation to whether the structure is captured). Fixed by having `StageInfoUI` additionally subscribe `WarClimaxWarmupStartedEvent`/`WarClimaxStateChangedEvent` and suppress that line whenever the active climax objective isn't `Annihilation`.
- **Testing gotcha (do not repeat):** publishing a synthetic `Stage.Events.StageClearedEvent`/`StageChangedEvent`/`CharacterDiedEvent` directly on the live `EventBus` during a Play Mode session — e.g. to verify an objective's completion path — is indistinguishable to `StageProgression`/`SaveService` from a real clear/death. `StageProgression` blindly advances its own tracked index on any `StageClearedEvent` (it doesn't check the event's payload against what's actually loaded), and `SaveService` writes every `StageChangedEvent` straight to `PlayerPrefs`. A synthetic event during testing genuinely advanced real save progress by one stage in this project's dev save. Prefer reflection-invoking the private method that owns the judgment logic directly (bypassing the EventBus) when verifying "did X correctly detect Y" during Play Mode.

### AF. Player Manual Control Toggle (implemented)
`Assets/02. Script/Character/` + `Assets/02. Script/UI/` — lets the player freely switch between the existing auto-chase movement and direct tap-to-move. Deliberately general-purpose and independent of War or any other system (not gated by climax state) — the toggle is always available.
- `Character.PlayerControlMode` enum `{ Auto, Manual }`; `Character.Events.PlayerControlModeChangedEvent`; `Character.PlayerControlModeService` (`IManager`/`IService`, registered in `GameBootstrapper`) holds the current mode, `Toggle()`/`SetMode()` publish the event on change.
- `Character.PlayerManualMover` (Player-only, `ITickable`) — on `Manual`, disables the Player's own `EnemyTracker` component to take over `CharacterMover.Target` (the exact same "disable the sibling tracker, drive `CharacterMover` directly" pattern `Soldier.SoldierBehaviorController` already uses for ranged kiting/off-screen return — see section W). On a new Input System pointer press that isn't over UI (`EventSystem.IsPointerOverGameObject()`), converts the tap's screen position to a world point (`Camera.ScreenToWorldPoint`) and points `CharacterMover.Target` at a scratch, unparented Transform placed there (`StoppingDistance = 0`) — unparented for the same reason `SoldierBehaviorController._returnAnchor` must be (section W: a child anchor's world position would recede as the character approaches it). On `Auto`, re-enables `EnemyTracker`. `Attacker` (auto-attack) is untouched by mode either way — only movement changes.
- `UI.PlayerControlModeToggleUI` — a persistent top-right button showing "자동"/"수동", positioned at Canvas sibling index 3 (ahead of every `*Popup` object) so any popup always draws on top of it rather than the toggle floating above open popups.

### AG. War Boss Unique Patterns (implemented)
`Assets/02. Script/War/Boss/` — gives the War-climax boss (currently chapter 3's `BossDefeat` objective) telegraphed AoE attacks on top of the normal `Monster_Boss` composition, without touching `Monster_Boss` itself — every other chapter's `s=40` finale boss is unaffected.
- `War.Boss.WarBossPatternSO` (data) — one pattern: `TelegraphDuration`, `Radius`, `Damage`. Pattern variety is pure data, not a C# type per pattern — `WarBossPattern_GroundSlam` (radius 2.5, damage 25, 1.5s telegraph) and `WarBossPattern_WideShockwave` (radius 4.5, damage 15, 2s telegraph) under `Assets/03. SO/War/`.
- `War.Boss.WarBossPatternRunner` (`ITickable`, `IPoolable`) — cycles its `patterns` array on `intervalBetweenPatterns`; each cast snapshots the nearest ally's (Player/Soldier layer) position at that instant, shows a `WarBossTelegraphIndicator` there, and after `TelegraphDuration` applies `Damage` via `Physics2D.OverlapCircleAll` + `Health.TakeDamage` to whatever is still standing in `Radius`. All judgment/timing is owned here, never by the indicator — the same `Attacker`/`WeaponSwing` split already used for melee (section H).
- `War.Boss.WarBossTelegraphIndicator` (`IPoolable`, pure visual) — a world-space `SpriteRenderer` whose circular sprite is generated once in code (`Texture2D`/`Sprite.Create`, no art asset) and shared (cached `static`) across every instance; `Show()`/`SetProgress01()` are its entire API, called only by the pattern runner.
- `WarBoss.prefab` — a duplicate of `Monster_Boss.prefab` (identical composition: `CharacterStatsProvider`/`Health`/`CharacterMover`/`MonsterTargetSelector`/`Attacker`+`MeleeAttackBehavior`/`BossMarker`/`HealthBar`/`MonsterLootProvider`/`StageMonsterScaler`) plus `WarBossPatternRunner` — composition reuse, no inheritance. `Stage_3_40.asset`'s boss spawn wave now references this prefab instead of `Monster_Boss`.

### AH. Skill System (implemented — supersedes the "SkillPanel is a scriptless placeholder" note in section M)
`Assets/02. Script/Skill/` — three fixed auto-cast skills the player levels up with gold + enhancement stones; deliberately does not know "Player" exists any more than Enhancement does:
- `SkillSO` — data asset: `DisplayName`, `Icon`/`IconTint` (all three skills currently reuse `WeaponSprite.png` tinted differently — same placeholder-reuse pattern as Player/Soldier/Cargo, no dedicated skill art yet), `Cooldown`, `MaxLevel`, a gold/stone cost curve (`GoldCostBase`/`GoldCostIncreasePerLevel`, `StoneCostBase`/`StoneCostIncreasePerLevel` — linear, same shape as `EquipmentEnhancementConfigSO`), and `GetMagnitude(level)` = `EffectValueBase + EffectValuePerLevel * level` (level 0 is an unenhanced baseline, not "no effect" — the skill always does something even before its first level-up).
- `SkillCatalogSO` — flat `SkillSO[]`, same `IndexOf`/`GetAt` shape as every other catalog in this project; lets `SaveService` identify a skill by index instead of an asset reference.
- `ISkillEffect.Execute(origin, magnitude)` — mirrors `Combat.IAttackBehavior`'s strategy pattern: `SkillSlot` never knows what a skill actually does, only when to call it. Three implementations under `Skill/Effects/`: `AreaDamageSkillEffect` (`Physics2D.OverlapCircleAll` around the caster, damages every live `Health` hit), `SingleTargetStrikeSkillEffect` (nearest live target in range), `SelfBuffSkillEffect` (temporary `AttackPower` bonus on the caster's own `CharacterStatsProvider`; re-casting reverts the previous bonus first instead of stacking, so repeated casts can't compound).
- `SkillSlot` (`MonoBehaviour`, `ITickable`) — one fixed slot. Caches `GetComponent<ISkillEffect>()` in `Awake`, accumulates `deltaTime`, and calls `Execute` every `SkillSO.Cooldown`, reading the current level from `SkillService` fresh on every cast (never caches it, so a mid-cooldown level-up takes effect on the very next cast). Player has 3 child GameObjects for this (`Skill_Whirlwind`/`Skill_BattleCry`/`Skill_PowerStrike`), each just a `SkillSlot` + one effect component + its own `SkillSO` reference — composition, not inheritance, same as every other domain here.
- `SkillService` (`IManager`/`IService`) — `TryLevelUp(definition)` requires both `Loot.CurrencyService.TrySpendGold` and `Equipment.EnhancementStoneService.TrySpendStones` to succeed (fails harmlessly with no partial spend if either is short), then publishes `SkillLeveledUpEvent`. Keeps only level state and never touches `RuntimeStats`/damage itself — same "service owns state, component applies effect" split as `EnhancementService`/`StatEnhancementReceiver`. Takes only `EventBus` as a constructor dependency (not `CurrencyService`/`EnhancementStoneService`) and looks those up via `GameBootstrapper.Services` at `TryLevelUp` time instead: both currencies are constructed *after* `SaveService.Load()` in `GameBootstrapper.Awake()` (so they can seed from the save), but `SkillService` itself must exist *before* `SaveService` (which takes it as a save/restore dependency) — requiring the currencies as constructor params would be a circular dependency.
- Save: `SkillService.SkillLevelSnapshot[]` (catalog index + level, same shape as `InventoryService.OwnedEquipmentSnapshot`) serialized as one JSON blob (`SaveData.SkillLevelsJson`), same pattern as the Inventory/equipped snapshot in section O. **Gotcha fixed:** `SkillLevelSnapshot` was originally missing `[Serializable]`, so `JsonUtility` silently produced an empty array on save — the level-up itself still worked (gold/stones were spent, the in-memory level went up), but the level silently reset to 0 on the next load, with no error anywhere. `JsonUtility` fails this particular case silently rather than throwing; worth checking for on any future save-blob struct.
- UI: `UI.SkillSlotBarUI` + `UI.SkillDetailPopupUI` (`Assets/02. Script/UI/`) are the real controllers that replaced the old scriptless "준비 중" `SkillPanel` mentioned in section M — same icon-bar-then-popup shape as `Equipment.EquippedSlotBarUI`/`EquipmentSlotPopupUI`, but simpler since a skill slot has no "which owned copy is equipped" concept, just one level per skill. `SkillSlotBarUI` shows `Lv.N` per slot and opens `SkillDetailPopupUI` on tap; the popup shows the current magnitude and the next level's gold/stone cost (or "MAX") and wires its 레벨업 button straight to `SkillService.TryLevelUp`.
- Not yet done: no Rank gate (Soldier gates at 십인 대장 via `requiredRank`, section U/Y; Skill has no such check and is active from game start), no cast VFX/SFX (melee at least has `WeaponSwing`; skills apply damage/buff with no visual tell), and the 3-slot loadout is fixed by design (no swapping/adding skills).

### AI. Content Editor: In-Editor Art & Catalog Tool (implemented, Editor-only)
`Assets/02. Script/Editor/` — a `Tools/Idle Project/Content Editor` window (opened via `[MenuItem]`) that consolidates two previously-manual jobs (assigning placeholder art, hand-editing catalog SO fields in the Inspector one asset at a time) into one place. Pure Editor tooling: no runtime code depends on anything here, and the `Editor/` folder name alone excludes it from player builds (no separate asmdef needed since the project has none).
- `ContentEditorWindow` (`EditorWindow`) — two tabs via `GUILayout.Toolbar`: **World Sprites** and **Catalog Editor**.
- **World Sprites tab** — a fixed list of `ArtAssignmentTarget`s, each a small polymorphic wrapper hiding *how* a sprite gets read/written for that particular kind of target (drag in a new `Sprite`, hit 적용, it writes straight to the real asset):
  - `PrefabSpriteTarget` — prefab assets (`Monster`/`Monster_Elite`/`Monster_Boss`/`Monster_GoldDungeon`/`WarBoss`/`Soldier`/`Soldier_Ranged`/`Cargo`/`Projectile`). Uses `PrefabUtility.LoadPrefabContents` + `SaveAsPrefabAsset` so it edits the prefab *asset* without needing Prefab Mode open.
  - `SceneSpriteTarget` — the `Player` GameObject, which (unlike the above) lives directly in the scene rather than as a prefab asset. Finds it by name in whatever scene is currently open, edits its `SpriteRenderer`, and marks the scene dirty; does nothing (safely) if that scene isn't open.
  - `ScriptableObjectIconTarget` — SO `icon` fields (`Skill_WhirlwindSlash`/`Skill_BattleCry`/`Skill_PowerStrike`, `Soldier_Melee`/`Soldier_Ranged`), via `SerializedObject`.
  - Equipment (150 assets) is deliberately **not** in this tab — too many for a flat manual list, handled by the Catalog Editor's searchable list instead. Stage (320 assets) is excluded entirely — that content is generated by a one-off script (section V), not meant for one-by-one hand editing. Animator Controller assignment isn't supported yet since no prefab in the project uses `Animator` at all (everything is a static sprite) — deferred until that exists.
- **Catalog Editor tab** (`CatalogEditorPanel` + `CatalogRegistration`) — generic editor for *any* of this project's catalogs (the "one SO holding a flat array of item SOs" shape used everywhere: `EquipmentCatalogSO`, `SoldierCatalogSO`, `SkillCatalogSO`, `RankCatalogSO`, `EquipmentGradeCatalogSO`, `SoldierEquipmentCatalogSO`, `BehaviorProfileCatalogSO`). `CatalogRegistration` is just `{ DisplayName, CatalogAssetPath, ArrayFieldName, ItemType }` — adding a catalog to the tool is one entry in `ContentEditorWindow.BuildCatalogPanel()`, no new code. Pick a catalog → search/click an item → every field on that item's SO renders via `SerializedObject` + `EditorGUILayout.PropertyField` (iterating `SerializedObject.GetIterator()`, skipping `m_Script`) — this is why the tool needs no per-field code at all: whatever fields a catalog's item SO has (string, enum, `Sprite`, nested arrays, object refs, ...) just show up and are editable, including fields added after this tool was built. "+ 새 항목 추가" creates a new SO instance next to the catalog asset and appends it to the catalog's array; "삭제" removes the selected item from the array and deletes its asset, behind an `EditorUtility.DisplayDialog` confirmation since it's not reversible.
- **Gotcha (deleting from an Object-reference array):** `SerializedProperty.DeleteArrayElementAtIndex` on an array of object references only *nulls out* the slot on the first call if the reference wasn't already null — it takes a second call to actually shrink the array. `CatalogEditorPanel.DeleteSelectedItem` checks `arraySize` after the first call and calls it again if the size didn't change; skipping this leaves a `null` gap in the catalog array instead of removing the slot.
- This tool directly motivated adding an `Icon` field to `EquipmentSO`/`SoldierEquipmentSO` (see section J) — both previously had no icon field at all (Equipment showed grade tint only), so there was nothing for the World Sprites tab (or the Catalog Editor's generic `PropertyField` rendering) to assign. Neither field is drawn by any real UI yet — this only gets the data in place.

### AJ. Equipment Enhancement Popup (implemented)
`Assets/02. Script/UI/` — replaces `EquipmentRowUI`'s old "tap 강화 → immediately call `TryEnhance`" behavior with a dedicated popup showing icon / success chance / level step / stat-by-stat preview / required materials before the player commits:
- `EquipmentEnhancementPopupUI` — `Open(OwnedEquipment target)`/`Close()`. Shows `target.Definition.Icon`, a **fixed** "성공 확률: 100%" string (the enhancement system has no success/fail roll — `TryEnhance` is deterministic once conditions are met — so this is static UI text, not derived from any config), `"{level}강 → {level+1}강"` (or `"{level}강 (MAX)"`), one `EquipmentStatPreviewRowUI` per stat from `EquipmentStatService.CalculatePreview(definition, level)` vs. `CalculatePreview(definition, level+1)`, a "필요 재료: 중복 장비 N개 + 강화석 M" line, and the enhance button itself (disabled + "MAX" label at max level). Subscribes `Inventory.Events.InventoryChangedEvent` and refreshes only when `evt.Changed == _target` and the popup is open — `TryEnhance` → `InventoryService.AddEnhancementLevel` already publishes this event, so the click handler itself doesn't need to call `Refresh()`. If the target line gets consumed to 0 while open (e.g. fused elsewhere as material), `Refresh()` closes the popup instead of showing a stale/invalid state.
- `EquipmentStatPreviewRowUI` — one row, "능력치명 현재값 → 다음값" via `StatDisplayNames.Get`/`FormatValue` (reused so % stats format correctly if equipment ever grants `AttackSpeed`/`CriticalChance`/etc., unlike `EquipmentDetailPopupUI`'s options list which formats raw `ToString("0.##")`).
- `EquipmentEnhancementService` gained `DuplicatesRequiredPerLevel`/`MaxLevel` passthrough getters (forwarding the already-public `EquipmentEnhancementConfigSO` fields) so the popup doesn't need its own serialized config reference — same "read state through the service" shape as `GetNextStoneCost`.
- `EquipmentRowUI.Initialize` gained an optional `Action<OwnedEquipment> onEnhanceRequested` parameter (default `null`, same shape as the existing `onDetailRequested`): if provided, the enhance button invokes it instead of calling `TryEnhance` directly. `EquipmentSlotPopupUI` wires it to `enhancementPopup.Open`, and its own `Close()` also closes `enhancementPopup` — same nested-popup lifecycle pattern as `detailPopup`.
- Scene wiring: `Canvas/EquipmentEnhancementPopup` (`PopupPanel` + close/icon/probability/level/stat-scroll-list/material/enhance-button children, built to match `EquipmentDetailPopup`'s dark-panel/gold-outline/Maplestory-Bold visual language) sits as a Canvas sibling right after `EquipmentDetailPopup`; `EquipmentSlotPopupUI.enhancementPopup` references it. `EquipmentStatPreviewRow.prefab` (new, `Assets/04. Prefab/`) is the stat-row prefab used by its scrollable `StatRowContainer` (same `ScrollRect → Viewport(RectMask2D) → Content(VerticalLayoutGroup + ContentSizeFitter)` shape as section AB's standard). Verified in Play Mode: opening a row's 강화 button opens this popup with correct icon/probability/level/stat-preview/material text, and closing `EquipmentSlotPopupUI` cascades to close it too.

### AK. Damage Numbers (implemented)
`Assets/02. Script/Combat/` — floating combat text shown at the moment damage lands, independent of the pooled visual/judgment split already used elsewhere:
- `Character.Health.TakeDamage(float amount, bool isCritical = false)` gained an `isCritical` parameter (default `false`, so the 6 call sites with no crit concept — skill effects, `WarBossPatternRunner`, `WarBattleController`'s lethal damage — needed no changes) and now publishes `Character.Events.DamageAppliedEvent(target, amount, isCritical)` alongside its existing `CharacterHealthChangedEvent`/death handling, same "Health owns damage application and its events" rule as always.
- `Combat.IAttackBehavior.Execute` gained a required `bool isCritical` parameter (not optional — this interface is Combat-internal, so a missing crit flag should fail to compile rather than silently default). `Combat.Attacker` (which already rolls the crit chance) passes it through to `MeleeAttackBehavior` (straight to `TakeDamage`) and `RangedAttackBehavior` → `Projectile.Launch(target, damage, isCritical)`, which stores it and applies it on arrival — same threading shape as the damage float itself.
- `Combat.DamageNumber` (`ITickable`, self-releasing like `Projectile` — not `IPoolable`, since `OnEnable`/`OnDisable` already coincide with `PoolManager`'s `SetActive` calls and there's no reuse state beyond what `Show()` resets) — a pooled `TextMesh`+`MeshRenderer` GameObject (no Canvas, cheap for frequent spawns), reuses `Assets/Font/Maplestory Bold.ttf`. `Show(worldPosition, amount, isCritical)` sets text/color; `Tick` lerps position upward and fades alpha over `DamageNumberConfigSO.Lifetime`, then self-releases via `PoolManager`.
- `Combat.DamageNumberConfigSO` — one shared data asset (`RiseDistance`, `Lifetime`, `SpawnHeightOffset`, `FontSize`, `NormalColor` = white, `CriticalColor` = red), referenced directly by the `DamageNumber.prefab`'s own serialized field (every pooled instance is a copy of the same prefab, so this needs no injection plumbing).
- `Combat.DamageNumberSpawner` (plain C# class, constructed in `GameBootstrapper.Awake()` right next to `LootDropper`, disposed in `OnDestroy` the same way) — subscribes `DamageAppliedEvent`, spawns/`Show()`s a `DamageNumber` at the target's current position via `PoolManager`. Doesn't distinguish attacker faction — it fires for any `Health.TakeDamage` call in either direction (monster hitting player included), which was a deliberate scope decision since `Health` itself is faction-agnostic; in practice only player/soldier hits ever show red since monster/boss `CriticalChance` defaults to `0`.
- `GameBootstrapper` gained a `damageNumberPrefab` field (assigned to `Assets/04. Prefab/DamageNumber.prefab`) and constructs/disposes `_damageNumberSpawner` alongside `_lootDropper`.
- Verified in Play Mode: real auto-combat produces rising/fading white numbers; a forced `TakeDamage(amount, isCritical: true)` renders red.

## 4. Execution Workflow (Strict Rule)
Do NOT write full implementation code at once. Follow this iterative approval process:

1. Requirements Analysis
2. Implementation Plan
3. Class Diagram / UML Outline
4. System Architecture Explanation
5. Target Folder/File Path Mapping
6. **PAUSE and Wait for User Approval.**

After approval: implement ONE atomic feature -> verify compile/errors (via UnityMCP `refresh_unity` + `read_console`) -> explain -> await next approval.

### Commit policy
Do NOT `git add`/`git commit` after implementing a feature by default. Only commit when the user explicitly says so (e.g. "커밋해줘", "커밋하고 진행해") — the user wants to review code before it's committed.

### Language rule
- Conversation with the user, and code comments (including XML doc comments), must be in Korean.
- Everything else — identifiers, class/variable/function names, file names — must never use Korean; English only.
